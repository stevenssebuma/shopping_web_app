# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
.vscode/launch.json

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
.ios/
.android/


# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Firebase configuration for your app
# This file contains your API keys and project IDs.
# For PUBLIC repositories, it is CRITICAL to ignore this file for security.
# For PRIVATE team repositories, some teams choose to commit it for convenience,
# but it's generally safer not to. If you commit it, ensure your repository
# is truly private and protected.
lib/firebase_options.dart

# Firebase-specific ignores
# firebase-debug.log files are generated by the Firebase CLI for debugging.
firebase-debug.log
firebase-debug.*.log